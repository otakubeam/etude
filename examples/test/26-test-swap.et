export {
    of Int -> String -> Int
    fun main argc argv;
}
fun swap a b = {
  var t = *a;
  *a = *b;
  *b = t;
};

fun main argc argv = {
  var x = 3; var y = 5;
  print("Orignal: %d %d\n", x, y);

  swap(&x, &y);
  print("Swapped: %d %d\n", x, y);

  assert(x == 5);
  assert(y == 3);

  0
};
