# Functions can take functions as parameters

fun takingBool(b: Bool) Int {
   if b { 1 } else { 0 }
}

fun takingFunction(f: (Bool) Int ) Int {
   f(true)
}

fun main() Unit {
   takingFunction(takingBool);
}
