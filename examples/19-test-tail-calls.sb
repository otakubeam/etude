
struct Size2 {
  a: Int,
  b: Int,
};

fun main() Unit {
  var p = new [2] Size2;

  var addrA = &(*(p + 0)).a;
  var addrAManual = p;

  var addrB = &(*(p + 0)).b;
  var addrBManual = addrA + 1;

  var secondElem = &(*(p + 1)).a;
  


  assert((addrB == addrA) == false);
  assert((addrB == addrBManual));
  assert((addrA == addrAManual));
  assert((addrB == secondElem) == false);
}
