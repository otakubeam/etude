
fun cycle(
  i: Int, last: *Int, 
  f1: Int, f2: Int, f3: Int
) Int {
  if i == 0 { return *last; };
  *last = *last+1;
  return cycle(i-1, last, f1, f2, f3);
}

fun main() Unit {
  var v = new Int;
  var result = cycle(4005, v, 1, 2, 3);
  assert(*v == 4005);
  assert(*v == result);
}
