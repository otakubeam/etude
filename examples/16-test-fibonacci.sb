fun fib(n: Int) Int {
  if n == 0 {
    return 0;
  };

  if n == 1 {
    return 1;
  };

  return fib(n-1) + fib(n-2);
}

fun main() Unit {
  # If the number is any bigger it takes too long
  assert(fib(17) == 1597);
}
