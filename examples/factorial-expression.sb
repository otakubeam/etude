# Atm, there is no built-in multiplication
# And also no cycles, all by hand

fun mul(a: Int, b: Int) Int {
   if a == 1 {
      b 
   } else {
      b + mul(a-1, b)
   }
}

fun factorial(n: Int) Int {
   if n == 0 {
      1
   } else {
      mul(n, factorial(n-1))
   }
}

factorial(3);
