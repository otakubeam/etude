#include <vm/memory/vm_memory.hpp>

// Finally,
#include <catch2/catch.hpp>

//////////////////////////////////////////////////////////////////////

TEST_CASE("vm:memory:block", "[vm:memory]") {
  vm::memory::VmMemory mem{};
  // mem.AccessMemory(vm::memory::MemAccess{});
}
